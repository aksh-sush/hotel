public class Hotel {
    private int hoted_id;
    private String 
    
}

im creating system design using oops, give me a very simple hotel magagement system system design with attributes of class diagram 
room:  
  classes: room number, type, floor, rate, status, amenities
  methods:
   isavailable(from date, to date), updatedtatus())
booking:
  classes: booking id, guestid, roomnumber, checkin, checkout, status
  method:   calculatecost(), cancel(), extendstay()  
guest:
  class: guestid, name, phno, email, bookings[]
  method: makebooking(), cancelbooking(),viewbill()
invoice:
  class: invoiceid, bookingid, roomcost, extras, taxes, total
  method: generate(),addextra(),applydiscount()
hotelmanager:
  class: managerid
  method: addroom(), setrates(),viewoccupancy(),resolveconflicts()

my usecase diagram:
    guest, receptiionist and manager are actors
    mainflow:
    1 guest provides checkin and out dates
    2 systems checks room availability
    3 system presents room with pricing
    4 guests select a room
    5 system creates booking id
    6 guest pay full or some
    7 on check in date room status changes to occupies
    8 on checkout date, final billing including extras happen

                         

